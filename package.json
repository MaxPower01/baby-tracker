{
  "name": "baby-tracker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "firebase deploy",
    "emulate": "firebase emulators:start"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
